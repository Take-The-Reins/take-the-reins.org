---
interface Props {
  href: string;
  primary?: boolean;
  target?: "_self" | "_blank";
}
const { href, primary = false, target = "_self" } = Astro.props;
---
<a
  class:list={[
    "px-4 py-2 transition",
    { "border border-stone-100 rounded-lg hover:bg-orange-700 hover:border-orange-700": primary }
  ]}
  href={href}
  target={target}>
  <span
    class:list={[
      "text-orange-50 font-semibold transition",
      { "hover:text-orange-300": !primary },
    ]}>
    <slot />
  </span>
</a>
